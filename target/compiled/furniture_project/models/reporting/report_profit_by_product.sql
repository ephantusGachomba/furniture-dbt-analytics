SELECT 
    PRODUCTID,
    PRODUCTNAME,
    CATEGORY,
    SUBCATEGORY,
    SUM(ORDERPROFIT) AS PROFIT
FROM FURNITURE_PROJECT.RAW.stg_orders
GROUP BY
    PRODUCTID,
    PRODUCTNAME,
    CATEGORY,
    SUBCATEGORY