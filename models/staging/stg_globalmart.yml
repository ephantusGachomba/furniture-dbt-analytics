version: 2

models:
  - name: stg_orders
    description: one unique order per row_orders
    columns:
      - name: ORDERID
        description: unique order id of the staging table.

      - name: SHIPMODE
        description: '{{ doc("shipmode") }}'
